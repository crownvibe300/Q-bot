# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS configuration)
CLIENT_URL=http://localhost:5173

# Note: Authentication is handled by Firebase on the frontend
# No backend authentication configuration needed
